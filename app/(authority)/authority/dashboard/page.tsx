import { AuthorityDashboard } from "@/components/civicai/authority/authority-dashboard";

export const metadata = {
  title: "Authority Dashboard",
  description: "Municipal command center for all EcoMind AI citizen reports.",
};

export default function AuthorityDashboardPage() {
  return <AuthorityDashboard />;
}
